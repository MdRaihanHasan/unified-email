.git
.github
.design
docs
node_modules
vendor

# Deployment-local bind mounts (pgdata is owned by the postgres uid and
# unreadable by the build context sender).
data
backups

# Built in the assets stage; a stale host copy must not win.
public/build
public/hot

storage/logs/*
storage/framework/cache/*
storage/framework/sessions/*
storage/framework/views/*
storage/app/attachments/*

# Secrets reach the container as environment, never baked into the image.
.env
.env.*
!.env.example

tests
phpunit.xml
